# Git Fetch

## Introduction

Downloads changes from the remote repository without merging them.

## Syntax

```bash
git fetch
```

## Example

```bash
git fetch origin
```

## Real World Use Case

Review remote changes before merging.

## Best Practices

Use `git fetch` before `git merge`.

## Interview Question

**Q:** Does `git fetch` modify your working directory?

**A:** No.